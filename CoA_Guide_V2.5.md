# 매출확정 프로세스 V2.5 - 전표 규칙(CoA) 완전 가이드

## 📋 개요

본 문서는 **매출확정(Revenue Recognition) 프로세스 V2.5**에서 발생하는 모든 회계 전표(Journal Entry)의 규칙, 조건, 금액 산출식을 정의합니다.

---

## 🔧 전표 규칙 핵심 정책

### 1. 계정 구조 (Account Structure)
- **자산(Asset)**: 보통예금, 상품, 미청구수익, 부가세예수금 등 (계정과목 10XX)
- **부채(Liability)**: 선수금, 미지급금, 부가세납부금 (계정과목 20XX)
- **수익(Revenue)**: 매출, 매출환입, 미청구매출 (계정과목 40XX)
- **원가(COGS)**: 매출원가, 매출원가(반품) (계정과목 50XX)
- **비용(Expense)**: 재고평가손실, 반품운송료 (계정과목 60XX)

### 2. 거래 유형별 전표 자동화 수준
| 거래 유형 | 트리거 | 자동화 수준 | 승인 주체 |
|----------|-------|----------|---------|
| ① 정상 매출확정 | 배송 완료 + 10일 경과 | 자동 배치 | 시스템 |
| ② 정상반품 환입 | 반품 승인(NORMAL) | 자동 생성 | 반품팀 승인 후 |
| ③④ 하자반품 환입 | QC 검수 완료 | 자동 생성 | 품질팀 검수 후 |
| ⑤ 반품거부 | 반품 거부(REJECTED) | 자동 처리 | 회계팀 |
| ⑥ 교환 | 교환 승인(EXCHANGE) | 자동 생성 | 교환팀 승인 후 |

### 3. 부가세 처리 원칙
- **매출 시점**: 부가세 포함 (Gross 방식)
- **환입 시점**: 환급액과 동시 처리 (부가세예수금 발생)
- **납부**: 매월 말 차감 처리

### 4. 재고 평가 방법
- **정상반품**: 100% 재고복구 (원가 기준)
- **하자반품(폐기)**: 100% 손실 처리
- **하자반품(재고복구)**: 평가절하(기본 50%) 후 부분 손실 처리

---

## 📊 거래 유형별 전표 상세 규칙

### ① 정상 매출확정 (Confirmed Sale)
**조건**: 배송 완료 후 10일 경과, 반품 요청 미존재

**시스템 프로세스**:
```
배치 프로세스 (Daily Batch at 00:00)
IF revenue_due_dt <= CURRENT_DATE
  AND NOT EXISTS(RETURN.status IN [REQUESTED, UNDER_REVIEW])
THEN
  CREATE REVENUE_CONFIRMED
  CREATE Journal Entries (자동)
END IF
```

**전표**:
| 전표# | 차변(Dr) | 대변(Cr) | 금액 | 설명 |
|------|---------|---------|------|------|
| 1-1 | 보통예금(1010) | 매출(4010) | 100,000 | 배송완료 매출인식 |
| 1-2 | 부가세예수금(1060) | 부가세납부금(2040) | 10,000 | 부가세 처리(반품기간 만료) |
| 1-3 | 미지급금(1050) | 선수금(2020) | 0 | 선수금 전환 완료 |

**금액 산출식**:
```
Sales Amount = ORDER_AMOUNT = 100,000
VAT = SALES_AMOUNT × 0.1 = 10,000
Total Amount = 110,000
```

**에러 핸들링**:
- ✓ 배치 중복 실행 방지 (REVENUE_CONFIRMED 상태 체크)
- ✓ 반품 상태 전체 확인 (REQUESTED 포함)
- ✓ 10일 초과 조건 검증 (달력일 기준)

---

### ② 정상반품 환입 (Normal Return Reversal)
**조건**: 배송 후 10일 이내, 정상반품 승인 완료

**시스템 프로세스**:
```
RETURN.APPROVED 트리거 (반품팀 승인 시)
IF RETURN.type = NORMAL
THEN
  UPDATE SO.status = COMPLETED
  CREATE Journal Entries (자동)
END IF
```

**전표**:
| 전표# | 차변(Dr) | 대변(Cr) | 금액 | 설명 |
|------|---------|---------|------|------|
| 2-1 | 매출환입(4020) | 미지급금(1050) | 100,000 | 매출 환입 |
| 2-2 | 부가세예수금(1060) | 부가세납부금(2040) | 10,000 | 부가세 환급 권리 |
| 2-3 | 상품(1020) | 매출원가(5010) | 80,000 | 반품 상품 재고 복구 |
| 2-4 | 반품운송료(6020) | 미지급금(1050) | 5,000 | 반품 배송료(회사부담) |

**금액 산출식**:
```
Return Amount = ORDER_AMOUNT = 100,000
VAT Refund = 10,000
Inventory Cost = COST_PER_UNIT × QUANTITY = 80,000
Return Shipping = 5,000
Total = 195,000
```

**에러 핸들링**:
- ✓ 반품 상태 확인 (APPROVED만 처리)
- ✓ 부가세 계산 검증 (10% 고정)
- ✓ 재고 가용성 확인 (재고 부족 시 예외 처리)

---

### ③④ 하자반품 환입 - 폐기 (Defect Return - Disposal)
**조건**: 배송 후 7일 이내, 하자반품 승인, QC 결과 폐기 판정

**시스템 프로세스**:
```
QC.result = DEFECT_CONFIRMED 트리거
IF RETURN.type = DEFECT
  AND QC.restock_yn = N (폐기)
THEN
  CREATE Journal Entries (자동)
  UPDATE INVENTORY (제거)
END IF
```

**전표**:
| 전표# | 차변(Dr) | 대변(Cr) | 금액 | 설명 |
|------|---------|---------|------|------|
| 3-1 | 매출환입(4020) | 미지급금(1050) | 100,000 | 매출 환입 |
| 3-2 | 부가세예수금(1060) | 부가세납부금(2040) | 10,000 | 부가세 환급 |
| 3-3 | 재고평가손실(6010) | 매출원가(5010) | 80,000 | 폐기 상품 손실(원가) |

**금액 산출식**:
```
Return Amount = 100,000
VAT Refund = 10,000
Loss Amount = INVENTORY_COST × QUANTITY = 80,000
Total = 190,000
```

**에러 핸들링**:
- ✓ QC 결과 확인 (DEFECT_CONFIRMED만)
- ✓ 폐기 여부 검증 (restock_yn = N)
- ✓ 손실금액 계산 (원가 기준)

---

### ④ 하자반품 환입 - 재고복구 (Defect Return - Restock)
**조건**: 배송 후 7일 이내, 하자반품 승인, QC 결과 재고복구 판정

**시스템 프로세스**:
```
QC.result = DEFECT_CONFIRMED 트리거
IF RETURN.type = DEFECT
  AND QC.restock_yn = Y (재고복구)
THEN
  damage_ratio = QC.damage_ratio (%) OR 0.5 (기본값)
  restock_cost = INVENTORY_COST × (1 - damage_ratio)
  CREATE Journal Entries (자동)
  UPDATE INVENTORY (평가절하로 복구)
END IF
```

**전표**:
| 전표# | 차변(Dr) | 대변(Cr) | 금액 | 설명 |
|------|---------|---------|------|------|
| 4-1 | 매출환입(4020) | 미지급금(1050) | 100,000 | 매출 환입 |
| 4-2 | 부가세예수금(1060) | 부가세납부금(2040) | 10,000 | 부가세 환급 |
| 4-3 | 상품(평가절하)(1030) | 매출원가(5010) | 50,000 | 평가절하 재고 복구(50%) |
| 4-4 | 재고평가손실(6010) | 매출원가(5010) | 30,000 | 손실(폐기 부분 20%) |

**금액 산출식**:
```
Damage Ratio = 50% (기본값, QC에서 조정 가능)
Restock Cost = 80,000 × (1 - 0.5) = 40,000
Loss Amount = 80,000 × 0.5 - 40,000 = 30,000
Total Entries = 100,000 + 10,000 + 40,000 + 30,000 = 180,000
```

**에러 핸들링**:
- ✓ QC 결과 확인 (DEFECT_CONFIRMED)
- ✓ 평가절하율 검증 (0~100%, 기본 50%)
- ✓ 재고 복구 가능성 확인

---

### ⑤ 반품거부 (Return Rejection)
**조건**: 하자반품 요청 거절 (NO_DEFECT_FOUND)

**시스템 프로세스**:
```
RETURN.status = REJECTED 트리거
IF RETURN.type = DEFECT
  AND QC.result = NO_DEFECT_FOUND
THEN
  -- 추가 회계처리 없음
  -- 기존 매출 유지
  UPDATE SO.status = COMPLETED
  NOTIFY CUSTOMER (반품 거절 사유)
END IF
```

**전표**:
| 전표# | 차변(Dr) | 대변(Cr) | 금액 | 설명 |
|------|---------|---------|------|------|
| 5-1 | - | - | - | **추가 처리 없음** |

**금액**: 없음 (매출 유지)

**에러 핸들링**:
- ✓ 반품 거절 사유 기록 (QC 결과)
- ✓ 고객 통보 로그 저장

---

### ⑥ 교환 (Exchange)
**조건**: 배송 후 10일 이내, 교환 승인

**시스템 프로세스**:
```
RETURN.type = EXCHANGE 트리거
IF RETURN.status = APPROVED
THEN
  -- Phase 1: 기존 주문 취소
  CREATE JE (매출환입)
  CREATE JE (재고 반품)
  
  -- Phase 2: 신규 주문 생성
  CREATE NEW SO (같은 금액, 다른 상품)
  CREATE JE (신규 매출)
  CREATE JE (신규 상품 원가)
END IF
```

**전표**:
| 전표# | 차변(Dr) | 대변(Cr) | 금액 | 설명 |
|------|---------|---------|------|------|
| 6-1 | 매출환입(4020) | 미지급금(1050) | 100,000 | 기존 주문 환입 |
| 6-2 | 상품(1020) | 매출원가(5010) | 80,000 | 기존 상품 반품 |
| 6-3 | 매출(4010) | 미지급금(1050) | 100,000 | 신규 주문 매출 |
| 6-4 | 매출원가(5010) | 상품(1020) | 80,000 | 신규 상품 원가 인식 |

**금액 산출식**:
```
Cancel Amount = 100,000
New Order Amount = 100,000
Inventory Cost = 80,000 (상품별로 다를 수 있음)
Net Impact = 0 (회계적 상쇄, 재고만 교체)
```

**에러 핸들링**:
- ✓ 기존/신규 주문 동시 처리 (순서 중요)
- ✓ 재고 교체 검증 (수량 및 상품코드)
- ✓ 중복 환불 방지 (미지급금 1회만)

---

## 📌 계정 코드 빠른 참조

| 계정 코드 | 계정명 | 용도 |
|----------|-------|------|
| **1010** | 보통예금 | 결제 및 환불 |
| **1020** | 상품 | 정상 재고 |
| **1030** | 상품(평가절하) | 하자 상품 평가절하 |
| **1050** | 미지급금(고객환불) | 환불액 미지급 |
| **1060** | 부가세예수금 | 부가세 환급 권리 |
| **2020** | 선수금(반품기간) | 배송 후 반품대기 선수금 |
| **2040** | 부가세납부금 | 납부할 부가세 |
| **4010** | 매출 | 정상 매출 |
| **4020** | 매출환입 | 매출 차감(환입) |
| **5010** | 매출원가 | 상품 원가 |
| **5020** | 매출원가(반품) | 반품 상품 원가 |
| **6010** | 재고평가손실 | 폐기/손실 상품 |
| **6020** | 반품운송료 | 반품 배송비 |

---

## ⚠️ 주요 주의사항

### 1. 반품 상태 전체 확인
- 매출확정 배치 실행 시 **RETURN.status IN [REQUESTED, UNDER_REVIEW, APPROVED]** 모두 확인
- REQUESTED 상태도 매출확정 제외 대상

### 2. 부분반품 처리
- SO Line Item 레벨에서 부분 수량 반품 시 **라인별로 상태 관리**
- 일부 라인만 반품되면 SO 전체 매출확정 제외

### 3. 부가세 환입 타이밍
- 매출환입과 **동시 처리** (별도 전표 아님)
- 부가세 환급은 **부가세예수금** 발생

### 4. 교환 vs 반품+신규주문
- 교환은 **동일 금액 기준** 취급
- 금액이 다르면 부가 결제/환불 처리 필요

### 5. 손실금액 계산
- 항상 **원가 기준** 손실 계산 (판매가 아님)
- 평가절하율은 QC에서 결정 (기본 50%)

---

## 🔄 시스템 통합 체크리스트

- [ ] 계정 코드 설정 (20개 계정)
- [ ] 거래 유형별 전표 템플릿 구성 (6개 유형)
- [ ] 배치 프로세스 설정 (매일 자정)
- [ ] 반품 상태 전체 확인 로직 구현
- [ ] 부가세 계산 검증 (10% 고정 OR 가변)
- [ ] 재고 복구 로직 연동
- [ ] 평가절하율 설정 (기본 50%)
- [ ] 에러 핸들링 및 로깅
- [ ] 전표 대사 및 검증 프로세스
- [ ] 월말 결산 통합

---

## 📞 문의 및 예외 처리

**시스템 담당**: 회계팀 / 개발팀  
**검증 담당**: 감사팀  
**승인 담당**: 회계팀장

예외 상황 발생 시 **반품 유형, QC 결과, 부가세 처리 방식** 명시하여 보고

---

*최종 수정일: 2025-11-06*  
*버전: V2.5*  
*작성자: SCM 차부장 시스템*
